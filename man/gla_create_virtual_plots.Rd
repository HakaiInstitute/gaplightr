% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lidar.R
\name{gla_create_virtual_plots}
\alias{gla_create_virtual_plots}
\title{Create virtual plots from LiDAR data}
\usage{
gla_create_virtual_plots(
  points,
  folder,
  output_dir,
  plot_radius,
  filter = "-keep_class 1 2 9",
  chunk_size = 0,
  batch_size = 1000,
  resume = TRUE,
  check_las_catalog = FALSE,
  ...
)
}
\arguments{
\item{points}{sf object with point locations}

\item{folder}{Directory containing LAS/LAZ files}

\item{output_dir}{Directory to save clipped plot files}

\item{plot_radius}{Radius of circular plots in meters}

\item{filter}{LAS filter string (default: "-keep_class 1 2 9")}

\item{chunk_size}{Chunk size for LAScatalog processing (default: 0)}

\item{batch_size}{Number of plots to process per batch (default: 1000).
Reduce if encountering memory errors with large datasets.}

\item{resume}{Skip points with existing output files (default: TRUE)}

\item{check_las_catalog}{Run las_check on catalog (default: FALSE)}

\item{...}{Additional arguments passed to readLAScatalog}
}
\value{
sf object with added las_files column containing paths to clipped plots
}
\description{
Clips circular plots from a LiDAR catalog at specified point locations.
Processes points in batches to avoid memory exhaustion with large datasets.
}
\details{
Processes plots in batches to prevent memory exhaustion. With large datasets
(e.g., 5000+ plots), processing all at once can fail. The batch_size parameter
controls how many plots are clipped simultaneously. Default of 1000 works well
for most systems. Reduce to 500 or lower if still encountering memory issues.
}
\examples{
\donttest{
  points_path <- system.file("extdata", "points.geojson", package = "gaplightr")
  dem_path <- system.file("extdata", "dem.tif", package = "gaplightr")
  las_folder <- system.file("extdata", "lidar", package = "gaplightr")

  points <- gla_load_points(points_path, dem_path)
  points <- gla_create_virtual_plots(
    points = points,
    folder = las_folder,
    output_dir = tempdir(),
    plot_radius = 50
  )
}

}
