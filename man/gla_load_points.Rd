% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/points.R
\name{gla_load_points}
\alias{gla_load_points}
\title{Load and validate observation points against a DEM}
\usage{
gla_load_points(x, dem, drop_na_dem = FALSE, ...)
}
\arguments{
\item{x}{Either a file path to any file that \code{sf::read_sf} can read, or an
sf object containing point geometries. If \code{x} contains a \code{point_id} column
of unique positive integers, those values are used as-is for all downstream
file naming; otherwise sequential IDs are assigned automatically.}

\item{dem}{Either a file path to a DEM raster file, or a \code{SpatRaster} object}

\item{drop_na_dem}{Logical. If \code{FALSE} (default), points falling on NoData
cells in the DEM cause an error. If \code{TRUE}, those points are dropped with
a warning and processing continues with the remaining points.}

\item{...}{Additional arguments passed to \code{sf::read_sf()} when \code{x} is a file path}
}
\description{
Loads point locations from a spatial file or sf object, validates them
against a DEM, and enriches each point with elevation, projected coordinates,
and WGS84 lat/lon. The result is the starting point for all downstream
gaplightr workflows.
}
\details{
This function performs strict validation:
\itemize{
\item Geometry type must be POINT
\item CRS must be defined and projected (not geographic lat/lon)
\item Point and DEM CRS must match exactly
\item All points must fall within DEM spatial extent
\item Points on NoData cells error by default; set \code{drop_na_dem = TRUE} to
drop them with a warning instead
}
\subsection{Point IDs}{

Every point is assigned a \code{point_id}, a positive integer used to name all
downstream output files (LAS clips, horizon CSVs, fisheye photos). If \code{x}
does not contain a \code{point_id} column, sequential IDs are assigned
automatically (1, 2, 3, ...). To use your own IDs (for example to preserve
cached outputs across re-runs, or to match an existing site numbering scheme),
include a \code{point_id} column containing unique positive integers before
calling this function.
}
}
\examples{
\donttest{
  points_path <- system.file("extdata", "points.geojson", package = "gaplightr")
  dem_path <- system.file("extdata", "dem.tif", package = "gaplightr")
  points <- gla_load_points(points_path, dem_path)
}

\donttest{
  # Supply your own point_id values to keep cached outputs stable across
  # re-runs or to match an existing site numbering scheme.
  dem_path <- system.file("extdata", "dem.tif", package = "gaplightr")
  my_points <- sf::st_read(
    system.file("extdata", "points.geojson", package = "gaplightr"),
    quiet = TRUE
  )
  my_points$point_id <- c(101L, 202L, 303L)  # user-defined IDs
  points <- gla_load_points(my_points, dem_path)
}

}
