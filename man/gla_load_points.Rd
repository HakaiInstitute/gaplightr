% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/points.R
\name{gla_load_points}
\alias{gla_load_points}
\title{Load points}
\usage{
gla_load_points(x, dem, ...)
}
\arguments{
\item{x}{Either a file path to any file that \code{sf::read_sf} can read, or an
sf object containing point geometries}

\item{dem}{Either a file path to a DEM raster file, or a \code{SpatRaster} object}

\item{...}{Additional arguments passed to \code{sf::read_sf()} when \code{x} is a file path}
}
\description{
Load points
}
\details{
This function performs strict validation:
\itemize{
\item Geometry type must be POINT
\item CRS must be defined and projected (not geographic lat/lon)
\item Point and DEM CRS must match exactly
\item All points must fall within DEM spatial extent
\item All points must have valid elevation values (no NoData cells)
}
\subsection{Point IDs}{

Every point is assigned a \code{point_id}, a positive integer used to name all
downstream output files (LAS clips, horizon CSVs, fisheye photos). If \code{x}
does not contain a \code{point_id} column, sequential IDs are assigned
automatically (1, 2, 3, ...). To use your own IDs (for example to preserve
cached outputs across re-runs, or to match an existing site numbering scheme),
include a \code{point_id} column containing unique positive integers before
calling this function.
}
}
\examples{
\dontrun{
  points <- gla_load_points("stream_points.gpkg", "dem.tif")
}

}
