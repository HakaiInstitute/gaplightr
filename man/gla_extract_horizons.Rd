% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/horizon.R
\name{gla_extract_horizons}
\alias{gla_extract_horizons}
\title{Extract horizon masks for multiple points}
\usage{
gla_extract_horizons(
  points,
  dem_path,
  output_dir,
  step = 5,
  max_search_distance = NULL,
  distance_step = NULL,
  camera_height_m = 1.37,
  parallel = TRUE,
  resume = TRUE,
  verbose = FALSE
)
}
\arguments{
\item{points}{sf object containing observation points with point geometry.
Must have x_meters and y_meters columns. Coordinates will be extracted from
the geometry and transformed to WGS84 if necessary.}

\item{dem_path}{Character path to the DEM raster file (GeoTIFF)}

\item{output_dir}{Character path to directory for caching horizon CSV files.
Each point's horizon data is saved as x_y_horizon.csv. Required parameter.}

\item{step}{Numeric azimuth step size in degrees for horizon calculation
(default: 5)}

\item{max_search_distance}{Maximum search distance in meters for horizon detection
(default: NULL, uses full DEM extent)}

\item{distance_step}{Distance step size in meters for sampling along line of
sight (default: NULL, uses raster resolution)}

\item{camera_height_m}{Camera height above ground in meters. The observer elevation
is calculated as ground elevation (from DEM) plus this height.}

\item{parallel}{Logical. If TRUE (default), use parallel processing via
\code{future.apply}. Set up parallel plan with \code{future::plan()} before
calling this function}

\item{resume}{Logical indicating whether to skip points that already have
cached horizon files (default: TRUE). When FALSE, recomputes all horizons.}

\item{verbose}{Logical indicating whether to print progress messages (default: FALSE)}
}
\value{
The input sf object with an added horizon_mask list-column. Each element
is a list containing:
\describe{
\item{x_msk}{Numeric vector of x-coordinates for horizon mask polygon}
\item{y_msk}{Numeric vector of y-coordinates for horizon mask polygon}
}
}
\description{
Extracts terrain horizon elevation angles from a DEM for multiple observation
points and converts them to horizon masks suitable for fisheye photo creation.
Supports caching to disk for efficient resume of interrupted workflows.
}
\details{
When \code{parallel = TRUE}, each worker loads the DEM independently from disk.
Set up a parallel plan before calling this function:
\code{future::plan(future::multisession, workers = 3)}
}
\examples{
\dontrun{
  points <- gla_load_points("points.gpkg", "dem.tif")
  points <- gla_extract_horizons(
    points = points,
    dem_path = "dem.tif",
    output_dir = "output/horizons"
  )
}

}
\seealso{
\code{\link[=gla_create_fisheye_photos]{gla_create_fisheye_photos()}} for using extracted horizons
}
