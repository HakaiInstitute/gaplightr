# Process lidar data for hemispherical photo creation

Process lidar data for hemispherical photo creation

## Usage

``` r
gla_transform_lidar(
  las_input,
  x_meters,
  y_meters,
  elev_m,
  camera_height_m,
  min_dist = 1
)
```
